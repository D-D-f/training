describe('Run test démo', () => {
    it('test', () => {
        const a = 2 + 2;
        expect(a).toBe(4);
    })

    it('vérifier si la fonction retourne le prenom', () => {
        expect(sayName('david').toBe('david');
    })
 })

// Permet de vérifié si le résultat obtenu est bien celui attendu
